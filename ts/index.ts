
document.addEventListener('DOMContentLoaded', () => {
    const root = document.getElementById('todo-list')

    if (root == null){
        console.log("failed to fetch todolist")
        return
    }
})